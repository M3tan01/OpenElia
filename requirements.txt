openai>=1.0.0
python-dotenv>=1.0.0
mcp>=1.0.0
python-nmap>=0.7.1
pydantic>=2.0.0
keyring>=24.0.0
docker>=6.1.0
networkx>=3.0
rich>=13.0.0
httpx>=0.24.0
cryptography>=42.0
fastapi>=0.110
uvicorn>=0.29
pytest>=8.0
pytest-asyncio>=0.23
# stix2 is extract-only (scripts/extract_actor_ttps.py), NOT a runtime dep.
# Install it via the optional extra when needed: pip install -e ".[extract]"
