# OpenElia Environment Configuration Template

# --- EXPENSIVE BRAIN ---
# Provider-agnostic: point EXPENSIVE_BRAIN_URL/KEY at any OpenAI-compatible
# endpoint (OpenAI, Anthropic, Gemini, a LiteLLM proxy, or self-host).
# See COMMANDS.txt Section 6 for detailed setup.
EXPENSIVE_BRAIN_URL=https://api.openai.com/v1
EXPENSIVE_BRAIN_KEY=your_key_here
EXPENSIVE_MODEL=gpt-4o
#
# Named providers (openai | anthropic | google) are an alternative to the
# generic slot above. Their API keys live ONLY in the OS keychain — set them
# via the CLI, never in this file:
#     python main.py model auth <provider> <api_key>

# --- SPECIALIZED API KEYS ---
SHODAN_API_KEY=your_shodan_key_here
VT_API_KEY=your_virustotal_key_here
ABUSEIPDB_API_KEY=your_abuseipdb_key_here
THEHIVE_URL=https://your-thehive-instance.com
THEHIVE_API_KEY=your_thehive_api_key_here

# --- OPERATIONAL CONSTRAINTS ---
# Authorized subnet for testing (RoE)
CYBER_RISK_INSTRUCTION="Strictly adhere to the defined scope. Execute only authorized operations. Never perform non-reversible destructive actions."
MAX_TOKEN_BUDGET=5.00
